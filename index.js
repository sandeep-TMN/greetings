function greeting() {
    const greetingMessage = "Hello from the tenMed network — empowering healthcare through innovation."
    console.log(greetingMessage)
    return greetingMessage
}

export default greeting